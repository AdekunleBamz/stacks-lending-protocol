# Stacks Lending Protocol — Tests

This repository contains tests for the Stacks lending protocol contracts.

CI: The project now runs Vitest on push and pull requests to the `master` branch using `.github/workflows/ci.yml`.

Local testing:

- Install dependencies: `npm ci`
- Run tests: `npm run test`
- Run tests with coverage: `npm run test:report`
